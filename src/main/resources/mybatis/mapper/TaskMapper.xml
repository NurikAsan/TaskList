<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nurikov.tasklist.repository.TaskRepository">

    <resultMap id="TaskResultMap" type="com.nurikov.tasklist.domain.task.Task" autoMapping="false">
        <id property="id" column="task_id"/>
        <result property="title" column="task_title"/>
        <result property="description" column="task_description"/>
        <result property="expirationDate" column="task_expiration_date"/>
        <result property="status" column="task_status"/>
    </resultMap>

    <select id="findById" resultMap="TaskResultMap">
        SELECT t.id as task_id, t.title as task_title,
        t.description as task_description, t.status as task_status,
        t.expiration_date as task_expiration_date
        from tasks t
        where id=#{id}
    </select>

    <select id="findAllByUserId" resultMap="TaskResultMap">
        SELECT t.id as task_id, t.title as task_title,
        t.description as task_description, t.status as task_status,
        t.expiration_date as task_expiration_date
        FROM tasks t
        JOIN users_tasks ut on t.id=ut.task_id
        where ut.user_id=#{userId}
    </select>

    <insert id="assignToUserId">
        insert into users_tasks(task_id, user_id)
        values(#{taskId}, #{userId})
    </insert>

    <update id="update">
        UPDATE tasks set title=#{title}, description=#{description},
        expiration_date=#{expirationDate}, status=#{status}?
        where id=#{id}
    </update>

    <insert id="create" keyProperty="id" keyColumn="id" useGeneratedKeys="true">
        INSERT INTO tasks(title, description, expiration_date, status)
        values(#{title}, #{description}, #{expirationDate}, #{status})
    </insert>

    <delete id="delete">
        DELETE FROM tasks where id=#{id}
    </delete>
</mapper>